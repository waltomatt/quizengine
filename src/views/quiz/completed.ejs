<!DOCTYPE html>
<html lang="en">
    <head>
        <%-include("../partials/includes") %>
        <title>Quiz completed</title>
    </head>

    <body>
        <div class="container">
            <div class="row mt-5">
                <div class="col-md-12">
                    <h2>Quiz completed!</h2>

                    <h4 class="text-primary mt-4">You scored: <%=result.score.count%>/<%=result.answers.length%> (<%=result.score.percentage%>%)</h4>
                    
                    <div class="alert alert-secondary mt-3">
                        <strong><%=result.meanAverage.percentage%>%</strong> of people completing this quiz scored between <%=result.meanAverage.min%>-<%=result.meanAverage.max%>%
                    </div>
                </div>
            </div>
        </div>

    </body>
</html>